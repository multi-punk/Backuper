[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/endstone_backuper"]

[project]
name = "endstone_backuper"
version = "0.1.0"

[project.entry-points."endstone"]
backuper = "endstone_backuper:BackuperPlugin"